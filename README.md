# Homebrew Tap for AI Router

Install [AI Router](https://github.com/soooooollee/ai-router) with Homebrew:

```bash
brew install soooooollee/tap/airoute
air init
air start
```

The formula is updated automatically when a new AI Router GitHub Release is published.
